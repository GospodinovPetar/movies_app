Django >= 4.0,<5.0
requests >= 2.26.0
pytz >= 2021.3
sqlparse >= 0.4.2
tzdata >= 2021.5  # Required for time zone support on Windows
gunicorn >= 20.1.0  # If deploying on a server (optional)
whitenoise >= 5.3.0  # For static file serving (optional)
